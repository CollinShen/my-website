# ================================================================
# .gitignore — these files/folders are NOT uploaded to GitHub
# ================================================================

# Secret environment variables — NEVER commit this
.env

# Node modules — too large to commit, restored by running "npm install"
node_modules/

# Mac system files
.DS_Store

# VS Code settings (optional — remove if you want to share your editor settings)
.vscode/
